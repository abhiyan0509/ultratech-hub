services:
  - type: web
    name: ultratech-hub
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python backend/qa_agent.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 10000
    plan: free
